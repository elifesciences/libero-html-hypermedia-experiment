{% extends 'base.html.twig' %}

{% block body %}

    <ol>
        {% for item in items %}
            <li>
                <a href="{{ path('item', {id: item.id}) }}">
                    {{ item.title|raw }}
                </a>
            </li>
        {% endfor %}
    </ol>

{% endblock body %}
